version: "3.9"

services:
  app:
    build: .
    container_name: my-node-app
    ports:
      - "8081:8081"
    env_file:
      - .env
    restart: unless-stopped
    command: ["node", "-r", "dotenv/config", "server.js"]
